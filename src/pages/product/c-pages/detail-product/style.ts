import styled from 'styled-components';

export const ListWrapper = styled.div`

  .ant-list-item {
    display: block;

    span {
      margin-right: 50px;
    }
  }

  .ant-image {
    margin-right:15px;
  }
  .img-item {
    .title {
      height: 200px;
    }
  }
  
  .detail-loading {
    position: absolute;
    left: 50%;
    top: 40%;
    transform: translate(-50%,-50%);
  }
  
`;
